# Use the official PostgreSQL image as the base image
FROM postgres:latest

# Expose the PostgreSQL port (default is 5432)
EXPOSE 5432

# This command is provided by the official image and will start the PostgreSQL server
CMD ["postgres"]